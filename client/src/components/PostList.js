import React from 'react';
import TweeterForm from './TweeterForm';
import Post from './Post'

// Prop = something that you pass inside the component, data passed
// in by parent
// This component will loop through the posts prop
class PostList extends React.Component {
  render() {

    // const Posts = this.prrops.posts.map(post => {
    //   // return <Post />
    //   return <li>{post}</li>
    // })

    return(
      <div>

        { /*    */ }

      </div>
    )
  }
}

export default PostList;
